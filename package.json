{
  "name": "glamcart-clone",
  "version": "1.0.0",
  "description": "Full-stack GlamCart.com e-commerce clone",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install:all && cd backend && npx prisma migrate dev --name init && node prisma/seed.js"
  },
  "workspaces": ["backend", "frontend"]
}
